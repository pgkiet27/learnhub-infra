apiVersion: v2
name: payment-service
description: LearnHub Payment Service - Payment Management
type: application
version: 0.1.0 # chart version (increase when change chart structure)
appVersion: "1.0.0" # app version (increase when deploy new version)